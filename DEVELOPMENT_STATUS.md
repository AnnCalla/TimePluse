# 当前开发进度

审阅时间：2026-07-09。

## 总体判断

项目已经完成可交互原型，计划中的第一阶段基本完成，第二、三、四阶段完成了主要 UI 和部分业务闭环，第五阶段只有全局快捷键和 Windows 构建雏形。数据层仍以浏览器 `localStorage` 为主，Go 后端只负责计时历史落盘，距离稳定发布仍有明显缺口。

## 已完成

### 应用框架与界面

- Wails v2.11.0、React 18、TypeScript、Vite、TailwindCSS 架构已经搭建。
- Windows 无边框、透明 WebView、Mica 背景已配置。
- 计时、计划、便签三个侧栏入口已实现。
- 支持绿色/灰色主题并通过 `localStorage` 记忆。
- 窗口最小化、最大化和关闭按钮已接入 Wails Runtime。

### 计时模块

- 支持番茄钟、自定义倒计时和正计时三种模式。
- 开始前强制填写任务名。
- 番茄工作、短休息、长休息及长休间隔可配置到秒。
- 番茄钟采用绝对截止时间计算剩余时长，窗口后台运行或渲染降频时不会按丢失的 interval 漂移。
- 支持当前轮次进度条、组内轮次提示和手动跳过工作/休息阶段。
- 支持分别配置自动开始休息、自动开始下一轮；设置通过 `localStorage` 持久化。
- 支持桌面通知、本地 Web Audio 完成提示音及可选轻微滴答声，不再依赖在线音频地址。
- 提供青柠、黑曜、珍珠、晴空和壁纸取色五套液态玻璃主题，背景、文字、玻璃表面和强调色同步切换。
- 支持暂停、重置、300×200 极简置顶模式。
- 极简模式使用完全透明背景，只保留时间与任务；整个窗口可拖动，双击恢复完整模式，并按主题选择黑/白高对比文字。
- 计划任务可把标题和预计时长带入计时器。
- 倒计时完成后触发提示音，写入 Go 后端历史，并向计划页发送完成事件。
- 正计时增加“完成并保存”操作，可以正常形成历史记录。
- Go 后端把历史写入 `%USERPROFILE%\Documents\TimePulse\data.json`。
- Go 后端提供历史读取接口；统计视图支持今天、本周、本月总工作时间、完成会话数、连续专注天数和近 12 周热力图。
- 统计只聚合已完成的番茄工作轮、自定义倒计时和正计时，番茄休息阶段不会写入历史或计入工作时长。

### 计划模块

- 任务包含标题、紧急度、重要度、截止日、分类和预计时长。
- 按紧急/重要两个维度划分四象限。
- 任务数据通过 `localStorage` 持久化。
- 可删除任务、从任务启动计时、完成后填写复盘。
- 当前运行期内可以接收计时完成事件并显示计时历史。
- 已有规则式“今日总结”占位内容。

### 便签模块

- 支持多便签创建、选择、编辑和删除。
- 支持 Markdown 与 GFM 预览。
- 便签数据和当前选择通过 `localStorage` 持久化。
- 支持粘贴剪贴板图片，并以内嵌 Data URL 写入 Markdown。
- 支持把主窗口切换成 320×260 的置顶便签模式。
- 悬浮模式支持透明度及纸张、白色、深色主题。

### 系统集成

- Windows 使用 Win32 `RegisterHotKey` 注册 `Ctrl+Alt+P`。
- 快捷键可显示、居中窗口并跳转便签页。
- 非 Windows 平台提供空实现，代码可以跨平台编译。
- 仓库包含 2025-12-10 生成的 Windows 可执行文件。

## 部分完成或存在行为缺口

- “悬浮便签”实际是缩放主窗口，不是计划中的独立多窗口便签；一次只能显示一个。
- 图片存储仍是 Data URL + `localStorage`，没有由 Go 保存到 `Documents\TimePulse\images`；大图容易触发浏览器存储额度限制。
- 计时历史虽由 Go 写入 JSON，但没有读取 API，应用重启后计划页不能显示该历史。
- 计划任务和便签只存在 WebView 的 `localStorage`，没有统一备份、迁移或数据版本机制。
- 提示音已改为本地 Web Audio 合成音，但仍不支持用户选择自己的音频文件。
- 今日总结按“任务截止日等于今天”筛选，而不是按实际完成时间；任务模型也没有完成时间字段。
- `SaveHistory` 忽略 JSON 解析、目录创建和文件写入错误，前端也没有等待或处理保存失败。
- 历史 ID 使用秒级 Unix 时间，同一秒完成多条记录时会冲突。
- 关闭前隐藏窗口的逻辑存在，但自定义关闭按钮直接调用 `Quit`；项目也没有托盘入口，隐藏后可能无法通过托盘恢复。

## 尚未完成

- AI 日报（OpenAI/Ollama）没有后端接口或前端调用。
- 系统托盘及“显示主窗口/退出”菜单未实现。
- 真正的多窗口 Sticky Note 未实现。
- 本地图片文件保存与 AssetServer 映射未实现。
- 用户自选提醒音未实现。
- 数据导入、导出、备份和恢复未实现。
- 安装包流程未验证；NSIS/UPX 当前未安装。
- 没有 Go、React 或端到端自动化测试。

## 验证结果

已在 Go 1.26.5 下执行：

```text
go test ./...  -> 通过（项目没有测试文件）
go vet ./...   -> 通过
wails version  -> v2.11.0
wails doctor   -> Go、Node.js/npm 和 WebView2 均正常
npm.cmd run build -> 通过
```

完整 `wails build` 未通过：Wails 2.11.0 在 Go 1.26.5 下生成绑定时扫描了 `frontend/node_modules`，将 `@babel` 等目录误判为 Go 导入路径。前端源码本身已通过 TypeScript 和 Vite 构建；完整桌面构建仍需解决 Wails/Go 版本兼容或跳过绑定重新生成。

## 建议的下一步顺序

1. 解决 Wails 2.11.0 与 Go 1.26.5 的绑定生成兼容问题，恢复完整桌面构建。
2. 处理 npm 审计报告的 9 个依赖漏洞，优先确认 3 个高危项是否影响生产包。
3. 修复正计时无法完成保存、后端错误被忽略、历史无法回读这三个业务闭环问题。
4. 统一计划、便签和历史的数据持久化，增加数据模型版本与备份。
5. 再实现本地图片、独立悬浮便签、托盘和 AI 总结。
6. 为计时状态机、四象限分类和持久化增加测试，再制作安装包。
